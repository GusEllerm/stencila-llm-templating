# Patterns ignored throughout this repo
.DS_Store
.envrc
.idea
.parcel-cache
.venv
lcov.info
node_modules

# Path specific patterns ignored
/target
/codecov
/codecov.*

# Cargo cache and build artifacts (but keep config files)
.cargo/
!.cargo/config.toml
!.cargo/audit.toml
